# A temporary directory for large file downloads (e.g., Hugging Face datasets)
TMPDIR=/path/to/your/tmpdir

# A directory for Hugging Face's cache
HF_HOME=/path/to/your/hf_home

# Directory where model artifacts (datasets, trained models, logs) are stored.
# This path is used by both the training scripts and the inference API.
MODEL_ARTIFACTS_DIR="/path/to/your/data/and/models"

# A secure, randomly generated key for authenticating API requests.
# Generate one using: openssl rand -hex 32
SECRET_API_KEY="your_secret_api_key_here"
